[Unit]
Description=phc2sys daemon

[Service]
ExecStart=/usr/bin/phc2sys -a -r -S 1.0
# Use the option -u < n > to print logs every n seconds
# For example, to print logs every minutes, add : -u 60

[Install]
WantedBy=multi-user.target
